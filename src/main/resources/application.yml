spring:
  application:
    name: fisherman
  config:
    import:
      - optional:file:.env[.properties]
  profiles:
    include:
      - datasource
      - security
      - redis
springdoc:
  use-fqn: true
