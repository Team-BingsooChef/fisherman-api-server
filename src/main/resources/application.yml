spring:
  application:
    name: fisherman
  config:
    import:
      - optional:file:.env[.properties]
  profiles:
    include:
      - datasource
      - security
      - mail
      - redis
      - security
springdoc:
  use-fqn: true
