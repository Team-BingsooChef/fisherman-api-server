spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:fisherman_test;MODE=MySQL;
    username: sa

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        allow-remote-access: true

logging:
  level:
    org.hibernate.SQL: trace
